# Agent 1 — Domain + Constraint Compiler

Build:
- canonical models
- axis/unit normalization
- legacy input adapter
- Constraint objects
- free-text normalization only at ingestion
- problem hash

Do not implement packing strategy.

Done:
all current SKU data can enter V2 without core algorithm reading Chinese requirement strings.
