# BLK-007F-8B — Structural Residual & Wall Interface Repair

## 状态

`BLK007F8B_REPAIR_STATUS = PASS`

`WALL_INTERFACE_REPAIR_READY = true`

`STRUCTURED_RESIDUAL_PACKING_READY = true`

`OPPORTUNISTIC_SINGLE_CARTON_FILL = disabled`

## 修复一：残余填充散乱

旧实现从每个箱体顶面的四个角生成单箱候选，并逐件提交。该策略虽然把14-SKU
结果从 `75.651930%` 推高到 `77.664761%`，但产生了棋盘式、齿状和孤立货物，
因此该结果已废止。

新实现只生成 `ResidualRegion → mixed-SKU RowPlan`：

- 顶部区域由同一高度的连续支撑区间合并得到。
- 地面候选必须形成横向完整行。
- 每个计划至少包含4件货物。
- 行覆盖率必须达到 `92%`。
- 同一行只允许深度、高度兼容的合法方向。
- 计划在克隆状态中通过完整GlobalValidator后原子提交。
- 禁止单箱、箱角锚点和部分行提交。

第二轮修复把被局部障碍分隔的左右肩部拆成独立连续区域，并允许多个相邻纸箱的
联合顶面构成真实支撑面。前端实际14-SKU清单原子提交13条完整货排、194件货物：
SKU-14 ×182、SKU-02 ×12。结果为 `1753 / 76.335484%`。旧版2—3箱的零散短柱
不再被接受；保留的货排覆盖率为 `96.04%—100%`。

合成场景证明：混合SKU顶部行可用；中心障碍两侧可分别形成完整地面行；多个下层
箱体可联合形成连续顶面。所有计划均为覆盖率100%的原子验证PASS。

## 修复三：SKU层数与异SKU顶载语义

`maxStackLayers` 现在只限制该SKU自身的连续堆叠层数。比如SKU-10限制3层时，
第4层SKU-10仍被硬拒绝，但其上可以放置满足方向、支撑、承压、类别、稳定性、
碰撞和边界约束的其他SKU。`allowStackingOnTop`、`maxBearingKg`、压力与类别规则
继续决定下层纸箱是否可以承载异SKU，未配置这些禁止项时不再被层数规则误伤。

## 修复二：单箱前沿污染

新增 `WallInterfaceRepairEngine`，按竖向货物列分析相邻墙面，而不是使用单一
`wall.max_x`。只有同一接口不同列的间距差超过5cm、且阻挡列占比不超过25%时，
才认定为单箱前沿污染。

- 锁定门墙绝不移动。
- 每个竖向列作为刚体移动，保持原支撑链。
- 只移动突出物旁边的肩部列。
- 统一墙间距不处理。
- 单列最大修复距离45cm。
- 每个接口基于原始几何判断，禁止修复结果沿整条墙链级联传播。
- 完整全局验证失败则整体回滚。

接口修复仍采用保守门槛：至少4个可比较列、实际阻挡列占比不超过25%，局部间距差
超过5cm才动作。最大单列移动0.45m；统一墙间距不动，GlobalValidator失败整体回滚。

本轮优先级是结构正确而非数量：SKU-10的3层限制只限制SKU-10自身；其上异SKU必须
重新通过支撑、承压、类别、稳定、碰撞及边界验证。最终实际清单通过完整验证。

## 回归

- Python：`332/332 PASS`。
- Frontend Node：`8/8 PASS`。
- overlap / penetration / OOB / hard violation：`0`。
- 门墙锁定、开门稳定和运输受力验证保持PASS。

完成后停止，未进入BLK-007F-8C。
